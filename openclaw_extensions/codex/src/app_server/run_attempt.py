"""Codex app-server run attempt runtime."""

from __future__ import annotations

from typing import Any


async def run_codex_app_server_attempt(params: dict[str, Any], options: dict[str, Any] | None = None) -> Any:
    raise RuntimeError("Codex app-server run attempt runtime is not available")
