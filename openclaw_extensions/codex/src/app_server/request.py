"""Codex app-server JSON request helpers."""

from __future__ import annotations

from typing import Any


async def request_codex_app_server_json(**_options: Any) -> Any:
    raise RuntimeError("Codex app-server request runtime is not available in this environment")
